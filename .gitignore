.env
.env.*
!.env.example
kaggle.json*
__pycache__/
venv/
.venv*/
.DS_Store
data/*
artifacts/
*.npy
# VPN client configs embed a private key.
*.ovpn
# SSH keys. This repo is public and `git add -A` would publish one.
*.pem
*.key
id_rsa*
# Competitive notes — never published.
private/
